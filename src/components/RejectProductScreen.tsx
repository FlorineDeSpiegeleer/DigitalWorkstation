import { MapPin, ChevronRight, AlertTriangle } from 'lucide-react';
import { IndustrialHeader } from './IndustrialHeader';
interface Props { onComplete: () => void; elapsedTime: number; productName: string; operatorSettings: { operatorName: string; line: string; station: string; }; onBack: () => void; onSettings?: () => void; }
export function RejectProductScreen({ onComplete, elapsedTime, productName, operatorSettings, onBack, onSettings }: Props) {
  return <div className="h-full min-h-0 flex flex-col bg-gray-100 overflow-hidden"><IndustrialHeader title="Afgekeurd product afvoeren" subtitle={`${productName} — afwijking gedetecteerd`} showTimer elapsedTime={elapsedTime} operatorSettings={operatorSettings} onBack={onBack} onSettings={onSettings}/><div className="flex-1 min-h-0 overflow-y-auto flex flex-col md:flex-row p-8 gap-8 max-w-[1280px] mx-auto w-full"><div className="flex-1 bg-white rounded-lg shadow-sm border border-gray-200 p-10 flex flex-col items-center justify-center text-center"><div className="w-24 h-24 bg-red-100 rounded-full flex items-center justify-center mb-6"><AlertTriangle className="w-14 h-14 text-red-600"/></div><h2 className="text-3xl text-gray-800 font-bold mb-2">{productName} afgekeurd</h2><p className="text-gray-500">Breng dit product naar de afkeurzone — niet terug in de gewone opslag.</p></div><div className="w-full md:w-96 flex flex-col gap-5"><div className="bg-red-50 border-l-4 border-red-600 rounded-lg p-6"><div className="flex items-center gap-3 mb-4"><MapPin className="w-6 h-6 text-red-600"/><h4 className="text-lg text-red-800 font-bold uppercase">Afkeurzone</h4></div><div className="space-y-2"><div className="flex items-center gap-3"><span className="text-sm text-red-600 font-medium w-20">Zone:</span><span className="text-lg text-red-900 font-bold">D</span></div><div className="flex items-center gap-3"><span className="text-sm text-red-600 font-medium w-20">Rek:</span><span className="text-lg text-red-900 font-bold">Afkeur</span></div></div></div><div className="bg-gray-50 rounded-lg border border-gray-200 p-5"><h4 className="text-xs uppercase tracking-wider text-gray-500 mb-3 font-medium">Volgende stap</h4><p className="text-sm text-gray-700 leading-relaxed">Label het product met de reden van afkeuring en meld dit bij de teamleader. Daarna kan je een nieuw exemplaar starten.</p></div><div className="flex-1"></div><button onClick={onComplete} className="w-full bg-red-600 hover:bg-red-700 text-white text-lg py-4 px-6 rounded-lg transition-colors flex items-center justify-center gap-3 font-medium shadow-sm">Afgevoerd — nieuw exemplaar starten<ChevronRight className="w-6 h-6"/></button></div></div></div>;
}
